# ********RoostGPT********
"""
Test generated by RoostGPT for test pythonHTest5 using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=leave_Add_whitespace_after_2a0fd1a30e
ROOST_METHOD_SIG_HASH=leave_Add_whitespace_after_cac8f567d9


```
Scenario 1: Testing the function with Add node including trailing whitespace
Details:
  TestName: test_add_node_with_trailing_whitespace
  Description: This test is intended to verify that the function properly handles the Add object with trailing whitespace and does not throw any errors or exceptions.
Execution:
  Arrange: Initialize an Add object with a trailing whitespace.
  Act: Call the leave_Add_whitespace_after function with the created Add object as parameter.
  Assert: Assert that there are no errors and the trailing whitespace remained unchanged.
Validation:
  As the function leave_Add_whitespace_after is designed to work with all Add objects, it should handle those with trailing whitespace correctly without modifying them or throwing exceptions. This test ensures that this business requirement is met. 

Scenario 2: Evaluating the behavior when provided with an Add node without a trailing white space
Details:
  TestName: test_add_node_without_whitespace
  Description: This test verifies the function's capability to accept and handle Add nodes without trailing whitespace.
Execution:
  Arrange: Create an Add node without trailing whitespace.
  Act: Invoke the leave_Add_whitespace_after function including the predefined Add node as an argument.
  Assert: Check that no error or exception was thrown and the Add node remains as it is, without any modifications to trailing whitespace.
Validation:
  Checking the function's ability to handle an Add node without trailing whitespace aligns with the requirement to handle all instances of Add nodes, ensuring that the leave_Add_whitespace_after function behaves as expected, regardless of whether trailing whitespace exists.

Scenario 3: Checking how function behaves when passing an Add node with varying whitespace characters
Details:
  TestName: test_add_node_various_whitespace
  Description: The aim of this scenario is to ensure the function can handle Add nodes where the trailing whitespace consists of various characters (spaces, tabs, newlines).
Execution:
  Arrange: Initialize an Add node with trailing whitespace composed of different characters.
  Act: Invoke leave_Add_whitespace_after while providing the initialized Add node as input.
  Assert: Ensure there are no errors or exceptions and the whitespace characters remain the same, indicating no modification took place.
Validation:
  The function should be able to handle Add nodes regardless of the composition of the trailing whitespace. This scenario ensures the business logic encapsulated in the function meets this requirement.
```
"""

# ********RoostGPT********
import pytest
from libcst._nodes.op import Add
from libcst._nodes.whitespace import SimpleWhitespace, TrailingWhitespace
from _typed_visitor import CSTTypedBaseFunctions


# setup pytest fixture
@pytest.fixture
def cst_typed_base_functions():
    return CSTTypedBaseFunctions()


def test_add_node_with_trailing_whitespace(cst_typed_base_functions):
    add_node = Add(whitespace_after=TrailingWhitespace(
        whitespace=SimpleWhitespace(value=' '))
    )
    # there should not have any exception while executing this function
    cst_typed_base_functions.leave_Add_whitespace_after(add_node)
    # Check whether whitespace value is still the same
    assert add_node.whitespace_after.whitespace.value == ' '


def test_add_node_without_whitespace(cst_typed_base_functions):
    add_node = Add(whitespace_after=None)
    # there should not have any exception while executing this function
    cst_typed_base_functions.leave_Add_whitespace_after(add_node)
    # As the whitespace was None initially, it should remain None
    assert add_node.whitespace_after is None


def test_add_node_various_whitespace(cst_typed_base_functions):
    add_node = Add(whitespace_after=TrailingWhitespace(
        whitespace=SimpleWhitespace(value=' \t\n'))
    )
    # there should not have any exception while executing this function
    cst_typed_base_functions.leave_Add_whitespace_after(add_node)
    # Check whether whitespace value is still the same
    assert add_node.whitespace_after.whitespace.value == ' \t\n'
